{
   "version": 2,
   "name": "expenses-app",
   "builds": [
      { "src": "index.js", "use": "@vercel/node", "config": {"maxDuration":"80000"}, 
      }
   ],
   "routes": [
      { "src": "/(.*)", "dest": "/" }
   ]
}
